import * as example from "./example"
import * as takeaway from "./Takeaway"

export const pageKeys = {
  ...example.pageKeys,
  ...takeaway.pageKeys
}

export const pageMetas = {
  ...example.pageMetas,
  ...takeaway.pageMetas
}
